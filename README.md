Pre-Project

Purpose:
1. Create a barebones website with HTML that has a main page that links to a few other pages, containing recipes.

Goals:
1. Basic HTML template
2. HTML < a href >, < strong>, < em>, < img src> tags

